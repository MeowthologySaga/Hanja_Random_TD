/*
 * 화면이 같은 일을 부르는 이름 (v042).
 *
 * 판이 무너진 사유를 종료 화면은 「수비 실패」, 도움말은 「게임오버」라고 불렀다.
 * 같은 일에 두 이름이 붙으면 플레이어는 그것을 두 가지 일로 배운다 — 특히 이 게임은
 * 지는 방식이 셋이라(적 한계 · 우두머리 제한시간 · 그 밖) 이름이 갈리면 「내가 왜
 * 졌는지」를 화면끼리 다르게 말하게 된다.
 *
 * **코어 문장은 담지 않는다.** core/content.ts·abilities.ts 가 만드는 문장은 그쪽이
 * 정본이고, 여기 있는 것은 화면이 자기 자리에 붙이는 낱말뿐이다.
 */

/** 판이 무너진 것을 부르는 이름 — 사유가 분명치 않을 때의 기본값이기도 하다. */
export const DEFEAT_LABEL = "수비 실패";

/** 적이 상한을 채워 진 경우. */
export const OVERRUN_LABEL = "적 한계 초과";

/** 마지막 우두머리를 제한시간 안에 못 잡아 진 경우. */
export const BOSS_TIMEOUT_LABEL = "제한시간 초과";
